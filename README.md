REST API made with express. It serves as an endpoint for my account-based fullstack web applications. 
It uses JWT (access,refresh tokens) to authorize users. The API needs to be paired with a database service to properly work,it uses prisma to make queries.
I usually pair this API with a react frontend and a postgres database, but it can be used with any frontend framework/library that can make outbound requests.

